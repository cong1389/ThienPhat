@using App.Domain.Common
@using App.Framework.Ultis
@using Resources
@model  App.FakeEntity.Meu.MenuLinkViewModel
@{
    ViewBag.Title = UI.CreateNew;
    ViewBag.Toolbar = new[] { "Save", "SaveNew", "Divider", "Back" };
}
@using (Html.BeginForm("Edit", "Menu", FormMethod.Post, new { data_container = ViewContext.RouteData.Values["controller"], enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(x => x.Id)   

    Html.RenderPartial("_CreateOrUpdate", Model);
    
}

@section scripts{
    <script type="text/javascript">

        //Copy value từ tên sản phẩm bỏ vào group SEO
        function CopyValue() {
            $("#Locales_0__MenuName").change(function () {
                var name = $("#Locales_0__MenuName").val();
                if (name != "") {
                    $("#Locales_0__MetaTitle").val(name);
                    $("#Locales_0__MetaKeywords").val(name);
                    $("#Locales_0__MetaDescription").val(name);

                    $("#MenuName").val(name);
                    $("#MetaTitle").val(name);
                    $("#MetaKeywords").val(name);
                    $("#MetaDescription").val(name);
                }
            });

            $("#Locales_1__MenuName").change(function () {
                var name = $("#Locales_1__MenuName").val();
                if (name != "") {
                    $("#Locales_1__MetaTitle").val(name);
                    $("#Locales_1__MetaKeywords").val(name);
                    $("#Locales_1__MetaDescription").val(name);
                }
            });

            $("#Locales_2__MenuName").change(function () {
                var name = $("#Locales_2__MenuName").val();
                if (name != "") {
                    $("#Locales_2__MetaTitle").val(name);
                    $("#Locales_2__MetaKeywords").val(name);
                    $("#Locales_2__MetaDescription").val(name);
                }
            });
        }

     $(function () {
         CopyValue();
     });
</script>
}
