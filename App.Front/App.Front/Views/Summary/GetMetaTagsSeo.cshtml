@model App.Domain.Entities.GlobalSetting.SettingSeoGlobal
@if (Model != null)
{
    if (!string.IsNullOrEmpty(Model.MetaTagMasterTool))
    {
        @Html.Raw(Model.MetaTagMasterTool)
    }
    if (!string.IsNullOrEmpty(Model.PublisherGooglePlus))
    {
        @Html.Raw(Model.PublisherGooglePlus)
    }
    if (!string.IsNullOrEmpty(Model.FbAdminsId))
    {
        @Html.Raw(Model.FbAdminsId)
    }
    if (!string.IsNullOrEmpty(Model.FbAppId))
    {
        @Html.Raw(Model.FbAppId)
    }
}
